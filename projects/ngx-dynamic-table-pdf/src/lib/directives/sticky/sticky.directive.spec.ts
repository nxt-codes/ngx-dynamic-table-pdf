import { StickyDirective } from './sticky.directive';

describe('SimpleStickyDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new StickyDirective();
  //   expect(directive).toBeTruthy();
  // });
});
